/** Shared copy for dashboard and analytics intervention metrics. */
export const REQUIRES_ATTENTION_LABEL = "Requires Attention";

/** Subtitle under the intervention count card; matches backend rating = 1 threshold. */
export const REQUIRES_ATTENTION_SUBTITLE = "Standards rated 1";
