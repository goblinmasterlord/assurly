# Example environment configuration
# Copy this to .env.local for local development overrides

# Backend API URL (REQUIRED in production)
# Development: Vite proxy handles /api/* requests
# Production: Must be set in Vercel Dashboard → Environment Variables
VITE_API_BASE_URL=https://assurly-frontend-400616570417.europe-west2.run.app

# Frontend URL (optional)
VITE_FRONTEND_URL=https://www.assurly.co.uk

# Debug API logging (optional)
# Development: true (enabled by default in .env.development)
# Production: false (disabled by default in .env.production)
VITE_DEBUG_API=false
