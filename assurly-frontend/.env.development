# Development environment configuration
# When running locally, we'll use Vite's proxy to avoid CORS issues

# Use relative path for API calls in development
# This will be proxied to the actual backend by Vite
VITE_API_BASE_URL=https://assurly-frontend-400616570417.europe-west2.run.app

# Enable debug logging
VITE_DEBUG_API=true